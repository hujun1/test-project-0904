# 专案工作流程

> 本档为 Obsidian vault（`E:\ai\teach-study`）内的详细笔记，位置 = 专案文件夹根目录。
> 与 `AGENTS.md`（精简版蓝图）互为对照：详细脉络在这里，跨 Agent 通用资讯在 `AGENTS.md`。

## 专案背景与详细脉络

本专案是一个**功能性测试专案**，专门用来验证 pi 生态的三个核心技能：

| 技能 | 用途 | 测试路径 |
|------|------|---------|
| `project-init` | 专案初始化 | ✅ 已验证（L1 + L2 + L3） |
| `startup` | 开工接续（读 handoff.md） | ✅ 已验证（git status / fetch / 跨电脑辨识全通） |
| `shutdown` | 收工同步（写 handoff.md + git push） | ✅ 已验证（本次即在收工） |

之所以独立成一个专案，而不是在某个业务专案里跑测试，是因为：

1. **隔离污染**：测试产生的杂物（`.gitignore`、repo 元件、笔记）不会污染业务专案
2. **可重置**：若测试失败 / 弄脏，可整目录删除重来
3. **路径示范**：刚好踩到 Obsidian vault 边界 = `E:\ai\teach-study` 这种「以父目录为 vault 根」的特殊场景

## 决策记录（为什么这样做）

### 决策 1：用「测试专案技能使用」作为专案名称

- **理由**：诚实表达意图，避免日后翻找时误以为是真实业务专案
- **取舍**：名称偏长，但内部沟通价值高过简洁

### 决策 2：把目标与时程留白

- **理由**：测试专案没有真实目标，硬填反而误导后续 Agent
- **取舍**：AGENTS.md 范本要求留白，所以符合规范

### 决策 3：路线图以「测试各项技能」为里程碑

- **理由**：让任何 Agent 接手时一眼就知道「这是测试骨架」，不会误把阶段一/二当成真实待办
- **取舍**：占用了「路线图」栏位，但比硬填假目标更安全

### 决策 4：Obsidian vault 路径 = 专案父目录

- **现状**：当前 vault = `E:\ai\teach-study`，本专案位于 `E:\ai\teach-study\test-project-0904`
- **影响**：L3 的「同名子文件夹」== 当前专案目录本身，`专案工作流程.md` 直接放根目录
- **优势**：笔记与专案档案物理同位置，换电脑备份时一起搬走

## 素材与相关笔记链接

- 技能路径：`C:\Users\lishuaibin\.pi\agent\skills\project-init\SKILL.md`
- pi 主文档：`C:\Users\lishuaibin\AppData\Roaming\npm\node_modules\@agegr\pi-web\node_modules\@earendil-works\pi-coding-agent\README.md`
- 相关 vault：`E:\my-know`、`E:\my-obs`、`C:\Users\lishuaibin\Documents\secondbrain`、`G:\我的云端硬盘\secondbrain`（这些是使用者个人的其他 vault，未在本专案使用）

## 🕳️ 踩坑笔记

### 坑 1：`gh` CLI 没在 PATH

- **现象**：在 bash 里跑 `gh` 报 `command not found`
- **原因**：装了 GitHub CLI 但 PATH 没自动加（在 `C:\Program Files\GitHub CLI\` 下）
- **解法**：用绝对路径 `/c/Program Files/GitHub CLI/gh.exe` 或 `where.exe gh` 找到
- **建议**：长期方案是把 `C:\Program Files\GitHub CLI\` 加进 PATH

### 坑 2：Obsidian 没有 MCP 整合

- **现象**：我手上没有「列目录 / 建立笔记」类 MCP，无法用工具直接写 vault
- **解法**：退而求其次，直接用 `write` 工具把 `专案工作流程.md` 写到 vault 内的专案根目录（物理位置就是 vault 的对应位置）
- **风险**：Obsidian 索引可能需要重启或重新扫描该档才会显示

### 坑 3：`$env:COMPUTERNAME` 在 Git Bash 不展开

- **现象**：在 bash 里写 `$env:COMPUTERNAME` 被当成字面字串（PowerShell 语法）
- **解法**：用 `hostname`（Git Bash 提供）或 `cmd.exe /c "echo %COMPUTERNAME%"`
- **结果**：本机电脑名 = `LAPTOP-KL01CRG6`

### 坑 4：Obsidian 装在非标准位置 `D:\hj\soft\tools\Obsidian\`

- **现象**：`winget list` 显示已装，但 `C:\Program Files\` 与 `AppData\Local\Programs\` 都找不到
- **原因**：使用者走自订路径（看到快捷方式 .lnk 才解开）
- **解法**：从 `C:\Users\Public\Desktop\Obsidian.lnk` 解析真实路径

## 🗓️ 最近更动记录

| 日期 | 事件 | 操作者 |
|------|------|--------|
| 2026-09-04 18:56 | 专案初始化（project-init L1）— 建 AGENTS.md + handoff.md | pi @ LAPTOP-KL01CRG6 |
| 2026-09-04 18:58 | 补建 L2（GitHub 私有 repo `hujun1/test-project-0904`）+ L3（本档） | pi @ LAPTOP-KL01CRG6 |
| 2026-09-04 19:02 | 跑 `startup` 技能验证（git status clean / 远端最新 / 同台电脑） | pi @ LAPTOP-KL01CRG6 |
| 2026-09-04 19:05 | 跑 `shutdown` 技能收工（更新 handoff + AGENTS + 本档 + commit + push） | pi @ LAPTOP-KL01CRG6 |

## 下一步建议（详细版）

1. **执行 `startup` 技能**：在本次对话的下一轮或新 session 触发，观察它能否正确读取 `handoff.md` 并判断状态
2. **执行 `shutdown` 技能**：在 startup 后触发，观察它能否正确回写 `handoff.md`、commit 并 push 到 GitHub
3. **若 L2 push 失败**：检查 `gh auth status` 是否仍登入、有无 proxy / 防火墙阻挡 `github.com:22` 或 `443`
4. **若 L3 vault 不显示本档**：在 Obsidian 里对该 vault 重新扫描，或重启 App